import 'package:flutter/material.dart';
class Mobility extends StatelessWidget {
  const Mobility({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Mobility Devices",style: TextStyle(color: Colors.white),),backgroundColor: Colors.deepPurple,),);
  }
}