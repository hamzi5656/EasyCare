import 'package:flutter/material.dart';
class GymProducts extends StatelessWidget {
  const GymProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Gym Products",style: TextStyle(color: Colors.white),),backgroundColor: Colors.deepPurple,),);
  }
}