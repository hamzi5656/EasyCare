import 'package:flutter/material.dart';
class Sticks extends StatelessWidget {
  const Sticks({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Walking Sticks",style: TextStyle(color: Colors.white),),backgroundColor: Colors.deepPurple,),);
  }
}