import 'package:flutter/material.dart';
class HearingAids extends StatelessWidget {
  const HearingAids({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Hearing Devices",style: TextStyle(color: Colors.white),),backgroundColor: Colors.deepPurple,),);
  }
}